# Учебный сайт о компании Intel

Образовательный веб-сайт с информацией о компании Intel и процессорах Intel с интеграцией ChatGPT API.

## Функциональность

- ✅ Главная страница с информацией о компании Intel
- ✅ История компании (5 ключевых событий)
- ✅ Преимущества процессоров Intel (6 карточек)
- ✅ Интерактивный слайдер поколений процессоров Intel
- ✅ Переключение языков: Русский (RU), Кыргызский (KG), Английский (ENG)
- ✅ Переключение темы (светлая/темная)
- ✅ Круглая кнопка с иконкой ИИ для задавания вопросов
- ✅ Интеграция с ChatGPT API (GPT-3.5) через сервер
- ✅ Полная адаптивность для всех устройств
- ✅ Современный минималистичный дизайн

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка API ключа

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Откройте файл `.env` и добавьте ваш OpenAI API ключ:
```
OPENAI_API_KEY=your_openai_api_key_here
PORT=3000
```

### Как получить API ключ OpenAI:

1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел API Keys
4. Создайте новый API ключ
5. Скопируйте ключ и вставьте в файл `.env`

### 3. Запуск сервера

```bash
# Обычный запуск
npm start

# Запуск с автоперезагрузкой (для разработки)
npm run dev
```

Сервер запустится на `http://localhost:3000`

## Структура файлов

```
├── index.html          # HTML структура сайта
├── styles.css          # Стили и дизайн
├── script.js           # JavaScript функциональность (фронтенд)
├── server.js           # Node.js сервер для обработки запросов
├── package.json        # Зависимости проекта
├── .env.example        # Пример файла с переменными окружения
├── .env                # Файл с переменными окружения (создать вручную)
└── README.md           # Документация
```

## API Endpoints

### POST /ask
Отправка вопроса к ChatGPT API

**Запрос:**
```json
{
  "question": "Что такое Intel Core i7?",
  "language": "ru"
}
```

**Ответ:**
```json
{
  "answer": "Intel Core i7 — это серия высокопроизводительных процессоров..."
}
```

### GET /health
Проверка состояния сервера

**Ответ:**
```json
{
  "status": "ok",
  "timestamp": "2025-01-15T10:30:00.000Z",
  "hasApiKey": true
}
```

## Технологии

### Фронтенд:
- HTML5
- CSS3 (с адаптивным дизайном)
- JavaScript (ES6+)

### Бэкенд:
- Node.js
- Express.js
- OpenAI API (GPT-3.5-turbo)
- CORS для кросс-доменных запросов

## Адаптивность

Сайт полностью адаптирован для:
- 🖥️ Десктоп (1200px+)
- 💻 Ноутбуки (992px - 1200px)
- 📱 Планшеты (768px - 992px)
- 📱 Мобильные устройства (< 768px, < 480px)

## Безопасность

- ✅ API ключ хранится на сервере (в `.env` файле)
- ✅ `.env` файл добавлен в `.gitignore`
- ✅ Валидация входящих запросов
- ✅ Обработка ошибок

## Разработка

Для разработки с автоперезагрузкой сервера:
```bash
npm run dev
```

Требуется установленный `nodemon`:
```bash
npm install -g nodemon
# или
npm install --save-dev nodemon
```

## Примечания

- API ключ OpenAI должен храниться в `.env` файле и никогда не коммититься в репозиторий
- Сервер обрабатывает запросы от фронтенда и отправляет их в OpenAI API
- Все данные о процессорах хранятся локально в JavaScript
- Сайт поддерживает 3 языка: русский, кыргызский, английский

## Лицензия

MIT
